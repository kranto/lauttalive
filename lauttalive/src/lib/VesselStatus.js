import React, { Component } from 'react';
import uuid from 'uuid';

class VesselStatus extends Component {

  constructor(x) {
    super();
    console.log('VesselStatus initialized', x);
  }

}

export default VesselStatus;
